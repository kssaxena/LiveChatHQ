import Header from "../utils/Header";
import HeaderAfterLogin from "../utils/HeaderAfterLogin";

const Contact =() => {
    return(
        <div><HeaderAfterLogin/></div>
    );
};

export default Contact;